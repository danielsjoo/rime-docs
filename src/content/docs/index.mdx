---
title: Rime
description: The fastest way to author polyglot data pipelines. The Editor is the front door; the CLI and YAML are the underlying truth.
template: splash
hero:
  tagline: "dbt-style pipelines across Python, R, JavaScript, and SQL. Declare your data retrievals and transforms in one file; Rime handles caching, logs, validation, outputs, and reports."
  actions:
    - text: Download the Editor
      link: /rime-docs/editor/overview/
      icon: right-arrow
    - text: Read the docs
      link: /rime-docs/get-started/quick-start/
      icon: open-book
      variant: minimal
    - text: View on GitHub
      link: https://github.com/danielsjoo/rime
      icon: external
      variant: minimal
---

import { Card, CardGrid, LinkCard, Tabs, TabItem } from '@astrojs/starlight/components';

## What is Rime?

Rime is a runtime for reproducible data work. You declare the pipeline once in `pipeline.dag.yaml`. Rime runs the graph, caches each node, captures logs, validates outputs, and writes artifacts.

```yaml
nodes:
  - id: raw_orders
    kind: sql
    source: queries/load_orders.sql

  - id: order_metrics
    kind: derive
    input: raw_orders
    columns:
      revenue: "[unit_price] * [quantity]"

  - id: sales_chart
    kind: python
    source: scripts/plot_sales.py
    in:
      orders: order_metrics
```

Here, the SQL node imports data. The derive node computes values with a simple expression syntax. The Python node graphs the data. Rime handles the cross-language handoff, persists and caches all outputs, and creates a runtime dashboard.

## Why Rime

<CardGrid stagger>
  <Card title="Functions, not jobs" icon="approve-check">
    A node is a function over dataframes — not a task that wires I/O. You write what each step computes; the runtime owns reading, writing, serialization, and language boundaries. The dbt mental model, extended past SQL.
  </Card>
  <Card title="One DAG, four languages" icon="puzzle">
    SQL for joins, Python for ML, R for stats, JavaScript for everything else. Same pipeline, named slots, typed boundaries. Dataframes cross language borders over Arrow IPC — zero-copy where possible, constant-time cast to pandas / tibble / DuckDB table.
  </Card>
  <Card title="Reproducible by default" icon="seti:lock">
    Content-addressed caching, deterministic outputs, freeze-able snapshots. Same script + same inputs = same artifact, every time. No "works on my machine."
  </Card>
  <Card title="Publishable narratives" icon="document">
    Render a publishable HTML report directly from your DAG. Tables, stats, stdout, figures, and node status — one render step, one document, one source of truth.
  </Card>
</CardGrid>

## How Rime is different

<Tabs>
  <TabItem label="Airflow / Prefect">
    - Airflow and Prefect orchestrate recurring jobs; Rime is local and one run.
    - Reads, writes, retries, errors, and persistence are usually coded inside tasks.
    - Rime owns dataframe handoff, execution order, caching, logs, validation, and outputs.

    <Tabs>
      <TabItem label="Airflow / Prefect">
        ```python
        @task
        def load_orders():
            orders = read_sql("SELECT * FROM orders")
            orders.to_parquet("outputs/raw_orders.parquet")

        @task
        def plot_sales():
            orders = pd.read_parquet("outputs/raw_orders.parquet")
            plot(orders)

        @flow
        def nightly_sales():
            load_orders()
            plot_sales()
        ```
      </TabItem>
      <TabItem label="Rime">
        ```yaml
        nodes:
          - id: raw_orders
            kind: sql
            source: queries/load_orders.sql

          - id: sales_chart
            kind: python
            source: scripts/plot_sales.py
            in:
              orders: raw_orders
        ```
      </TabItem>
    </Tabs>
  </TabItem>
  <TabItem label="Hex">
    - Hex is a proprietary notebook-style workspace; Rime is open source and file-backed.
    - Rime projects are portable `pipeline.dag.yaml` files.
    - Rime's Directed Acyclic Graph (DAG) is made of functions, not inline notebook modifications.

    <Tabs>
      <TabItem label="Hex">
        ```python
        # SQL cell: raw_orders
        SELECT *
        FROM warehouse.orders

        # Python cell: order_metrics
        order_metrics = raw_orders.copy()
        order_metrics["revenue"] = (
            order_metrics["unit_price"] * order_metrics["quantity"]
        )

        # Python cell: sales_chart
        plot_sales(order_metrics)
        ```
      </TabItem>
      <TabItem label="Rime">
        ```yaml
        nodes:
          - id: raw_orders
            kind: sql
            source: queries/load_orders.sql

          - id: order_metrics
            kind: derive
            input: raw_orders
            columns:
              revenue: "[unit_price] * [quantity]"

          - id: sales_chart
            kind: python
            source: scripts/plot_sales.py
            in:
              orders: order_metrics
        ```
      </TabItem>
    </Tabs>
  </TabItem>
  <TabItem label="Snakemake">
    - Snakemake dependencies are based on files; Rime dependencies are based on nodes.
    - Reads, writes, errors, and data persistence are usually coded manually in rules or scripts.
    - Snakemake freshness is based mainly on file timestamps and metadata, not runtime-managed node identity.

    <Tabs>
      <TabItem label="Snakemake">
        ```python
        rule load_orders:
            output: "outputs/raw_orders.parquet"
            shell: "python scripts/load_orders.py {output}"

        rule plot_sales:
            input: "outputs/raw_orders.parquet"
            output: "outputs/sales.png"
            shell: "python scripts/plot_sales.py {input} {output}"
        ```
      </TabItem>
      <TabItem label="Rime">
        ```yaml
        nodes:
          - id: raw_orders
            kind: python
            source: scripts/load_orders.py

          - id: sales_chart
            kind: python
            source: scripts/plot_sales.py
            in:
              orders: raw_orders
        ```
      </TabItem>
    </Tabs>
  </TabItem>
</Tabs>

## Choose your surface

The Editor and the CLI both consume the same `pipeline.dag.yaml`. Start visually, drop to YAML, or run the same project in CI.

<CardGrid>
  <LinkCard
    title="Rime Editor"
    description="Visual DAG authoring, live table previews, data diffs, and no-code core-node workflows."
    href="/rime-docs/editor/overview/"
  />
  <LinkCard
    title="What is Rime?"
    description="The two-minute pitch: core nodes, script functions, reports, and where Rime fits."
    href="/rime-docs/get-started/what-is-rime/"
  />
  <LinkCard
    title="Two ways to use Rime"
    description="Learn when to use core nodes, when to use script nodes, and what the YAML file is doing."
    href="/rime-docs/get-started/two-ways-to-use-rime/"
  />
  <LinkCard
    title="Quick start"
    description="Author your first DAG in 10 minutes. Core nodes, script nodes, and HTML report, end to end."
    href="/rime-docs/get-started/quick-start/"
  />
</CardGrid>

## Real pipelines you can clone

<CardGrid>
  <LinkCard
    title="Cars × CO₂ emissions"
    description="SQL source + JS API fetch + Python UMAP + R regression → HTML narrative. The canonical multi-language example."
    href="/rime-docs/examples/cars-emissions/"
  />
  <LinkCard
    title="Penguin classifier"
    description="Single-file teaching pipeline. Pivot, derive, t-test, plot — the smallest interesting DAG."
    href="/rime-docs/examples/penguins/"
  />
  <LinkCard
    title="Embed in Node"
    description="Use @rimekit/runtime programmatically from any Node script. For headless contexts and CI plugins."
    href="/rime-docs/examples/headless/"
  />
  <LinkCard
    title="DuckDB single source"
    description="Minimal SQL-only pipeline. Good for ad-hoc warehouse reports without leaving the SQL world."
    href="/rime-docs/examples/sql-only/"
  />
</CardGrid>
